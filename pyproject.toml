[build-system]
# setuptools is used instead of hatchling to support the optional sdm_cpp
# pybind11 C++ extension.  pybind11 and wheel are build-time only.
requires = ["setuptools>=77", "pybind11>=2.13", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "shelxfile"
dynamic = ["version"]
authors = [
    { name = "Daniel Kratzert", email = "dkratzert@gmx.de" },
]
description = "A parser for SHELXL results files"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE.md"]
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Topic :: Scientific/Engineering :: Chemistry",
    "Development Status :: 5 - Production/Stable",
]
dependencies = ["numpy>=1.21"]

[project.optional-dependencies]
gui = ["qtpy"]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "ty",
    "qtpy",
    "PyQt6",
    "pytest-qt",
]

[project.urls]
"Homepage" = "https://dkratzert.de/shelxfile.html"
"Bug Tracker" = "https://github.com/dkratzert/ShelXFile/issues"

[tool.setuptools.dynamic]
version = {attr = "shelxfile.version.VERSION"}

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "corpus: test requires the external structure corpus (--corpus PATH or $SHELXFILE_CORPUS); skipped otherwise.",
]

[tool.setuptools.packages.find]
where = ["src"]
include = ["shelxfile*"]
namespaces = false

[tool.setuptools.package-data]
"shelxfile" = ["py.typed"]
"shelxfile.cif" = ["*.tmpl"]
"shelxfile.atoms.atomic_weights" = ["LICENSE", "README.md"]

[tool.cibuildwheel]
# Build wheels for Python 3.12–3.15; skip 32-bit targets
skip = ["*-win32", "*-manylinux_i686"]
build = ["cp312-*", "cp313-*", "cp314-*", "cp315-*"]
# Tests are run by the separate `tests` job; skip per-wheel testing here
test-skip = "*"

[tool.cibuildwheel.macos]
# universal2 covers both arm64 and x86_64 on a single macOS runner
archs = ["universal2"]

